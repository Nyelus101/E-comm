# python3 -c "import secrets; print(secrets.token_hex(32))"




## Step 4 — requirements.txt

# backend/requirements.txt

# ─── Web Framework ───────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6

# ─── Database ────────────────────────────────
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.9       # PostgreSQL driver
pgvector==0.3.2

# ─── Auth & Security ─────────────────────────
# python-jose[cryptography]==3.3.0   # JWT tokens
# passlib[bcrypt]==1.7.4             # Password hashing
bcrypt==4.2.0
PyJWT==2.9.0
python-multipart==0.0.12           # File uploads

# ─── Validation ──────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2           # Settings from .env

# ─── Email ───────────────────────────────────
# resend==2.4.0
mailersend==0.5.8

# ─── Image Storage ───────────────────────────
cloudinary==1.41.0
python-slugify==8.0.4

# ─── Caching ─────────────────────────────────
redis==5.1.1

# ─── Payment ─────────────────────────────────
# stripe==11.1.0
# (Paystack uses httpx — no extra package needed)

# ─── HTTP Client (for external APIs) ─────────
httpx==0.27.2


email-validator==2.0.0


# ─── Search ──────────────────────────────────────
# elasticsearch==8.13.0
# ─── Search — Typesense (replaces elasticsearch) ─────────────────────────────
typesense==0.21.0


# ─── AI / LangGraph ──────────────────────────────────────────
# groq==0.11.0                     # Groq LLM API (free)
# sentence-transformers==3.0.1     # Local embeddings (free)
# torch==2.4.1                     # Required by sentence-transformers
# numpy==1.26.4
# langgraph==0.2.28
# langchain-core==0.3.15
# langchain-core>=0.3.75,<0.4

# ─── Commented out — switch back when needed ─────
# anthropic==0.37.1              # Claude API
# langchain-anthropic==0.2.4     # LangChain Claude integration
# (Voyage AI uses httpx — no package, just API key)


# ─── Embeddings — Voyage AI official SDK (replaces raw httpx calls) ──────────
# voyageai==0.3.7
# numpy==1.26.4